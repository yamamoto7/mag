<!DOCTYPE html>
<html>
  <head>
    <title>Agell</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

  </head>

  <body>
    <%= hidden_field_tag "authenticity_token", form_authenticity_token %>
    <div id="app">
      <%= yield %>
    </div>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
